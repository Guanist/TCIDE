# TCIDE LSP 单行损坏修复 — 工作记录

## 时间
2026-08-18 深夜（GMT+8）

## 背景
在推进「真超级 IDE」改造计划的 Step 1（真 MCP 外部连接层）完成后，进入 Step 2（LSP 诊断桥接）时，发现 `src/main/lsp-manager.ts` 无法正常编译。

## 问题根因
`src/main/lsp-manager.ts` 被写坏成**单行**（12860 字符、0 个换行符）。

单行化的致命后果：文件内的 `// ── 类型定义 ──`、`// ── 服务器配置表 ──` 等**行注释**会从 `//` 一直吃到文件末尾（因为没有任何换行符来终止行注释），把后面的所有代码（类型定义、LSP_CONFIGS、class LspManager、单例导出）全部吞成注释。

结果：tsc 编译该文件只输出 77 字节的空模块头（`"use strict"; Object.defineProperty(exports, "__esModule"...`），`dist/main/lsp-manager.js` 为空 → **LSP 在打包产物里从未真正生效过**。

之前一直挂着的 `TS2305: Module '"./lsp-manager"' has no exported member 'lspManager'` 历史编译噪音，正是这个空模块导致的连锁反应。

## 关键验证
- 坏文件：12860 字符、0 换行
- 干净文件（git 历史 `0277564` / v1.4.0）：13297 字符、437 个 LF
- `13297 − 437 = 12860` → **内容 100% 一致，仅换行符被抹掉**，无损恢复

## 恢复方法
- 用 node `cp.execFileSync('git', ['show', '0277564:src/main/lsp-manager.ts'])` 取原始字节（**不能**用 PowerShell `>` 重定向，会把 UTF-16/UTF-8 字节搞坏）
- 检测 BOM（FF FE = UTF-16 LE），按 `utf16le` 解码；否则按 utf8
- 恢复版无乱码、无 `@ts-nocheck`、中文正常
- 写回 `src/main/lsp-manager.ts`（UTF-8）

## 修复结果
- 主进程 tsc 编译 **0 错误**（TS2305 噪音彻底消失）
- `dist/main/lsp-manager.js` 完整输出 15665 字节
- 含 `lspManager`、`class LspManager`、`publishDiagnostics` 全部关键符号

## 提交
- `40505d6` fix: 修复 lsp-manager.ts 单行损坏导致编译产物为空(LSP 失效)（437 insertions, 2 deletions）

## 相关已完成
- Step 1 真 MCP 外部连接层：提交 `f36bbc7`，UTF-8 帧修复（buffer 按字节累积，修复中文响应字节数<字符数导致的帧永不解析）

## 待办
1. Step 2：`publishDiagnostics` 桥接到 renderer（诊断有但 Monaco 不显示）
2. Step 3：vector-indexer 接项目级长期记忆
3. Step 4：Agent 改完自动验证
4. Step 5：reviewer 回灌闭环
5. 用户本机 push 覆盖远程 main `22fc3c3`（共建者 LK-BLOG 乱填的提交）
