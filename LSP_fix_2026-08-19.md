# TCIDE 全局脚本缺失根因修复 — 2026-08-19

## 目标
修复 TCIDE「设置/模型面板点不动、报 `$id is not defined` / `showToast is not defined`」这类反复出现的故障根因。

## 根因定位（两个叠加）

### 1. 构建管线每次 build 都清空全局脚本（真凶）
- `dist/renderer/index.html` 末尾由 `vite.config.ts` 的 `petPlugin.transformIndexHtml` 注入三个 `<script src="./assets/p0-modules.js">` / `p1` / `p2`。
- 这三个脚本是「经典全局脚本」，定义应用与内联 `onclick` 依赖的全局：`$id`、`$$`、`on`、`showToast`、`showLoading`、`hideLoading`、`toggleZenMode`，以及 `window.__tcide_*` 系列桥接。
- `vite.config.ts` 的 `closeBundle` 原本从 `dist/renderer/assets_save/` 目录恢复它们，但该目录**从未存在** → 每次 `npm run build`（`emptyOutDir: true`）清空 assets 后，p0/p1/p2 全部丢失。
- 这就是 MEMORY.md 里反复记录「模型连不上、点不动」的根因。

### 2. p0/p1 在 git 中是乱码（GBK 破坏中文字符串字面量）
- HEAD 里 `dist/renderer/assets/p0-modules.js`(13123B) / `p1-modules.js`(33210B) 的中文字符串字面量被 UTF-8 当 GBK 读导致乱码（`鏂板缓鏂囦欢` 等）。
- 干净版本在历史中：p0 @ `8bf8c3f`（12859B，mojibake=0），p1 @ `2351757`（31076B，mojibake=0）。p2 是重建桩，HEAD 版本就是干净的（1490B）。
- 校验：干净 p0/p1 与当前 recovered 版本在「全局符号集合」上完全一致（p0: `$id/showToast/showLoading/hideLoading` + 6 个 `__tcide_*`；p1: `$$/on/toggleZenMode/enterZen/exitZen` + 5 个 `__tcide_*`），仅在注释/结构上略优。

## 已执行的修复
1. 从 git 历史恢复干净 p0/p1/p2 到 `recovered-modules/` 和 `dist/renderer/assets/`（脚本 `restore_clean.js`，用 Node 读 `git show` 字节级提取，去 BOM，避免 PowerShell 重定向破坏字节）。
2. 修改 `vite.config.ts` `closeBundle`：改为从 `recovered-modules/`（git 跟踪、干净 UTF-8）恢复三个脚本，替代不存在的 `assets_save/`。

## 验证
- `npm run build`（vite v5.4.21，966 modules，7.90s）成功。
- 构建后 `dist/renderer/assets/` 中 p0(12859B)/p1(31076B)/p2(1490B) 均存在。
- `index.html` 含全部三个 `<script>` 引用。
- 三个脚本 `new Function(s)` 语法校验通过，mojibake=0。
- 源码 `main.ts` 确实依赖这些全局（`showToast`×81、`on(`×132、`acceptDiff`×4、`closeDiffModal`×5 等）。

## 待办（未执行，需用户决定）
- 本次改动尚未 commit/push。改动文件：
  - `M vite.config.ts`
  - `M dist/renderer/assets/{p0,p1,p2}-modules.js`
  - `M recovered-modules/{p0,p1}-modules.js`、`?? recovered-modules/p2-modules.js`
- 其余未动的既有工作区改动（agent-orchestrator、ipc-handlers、preload、package.json、dist/main/handlers 等）保持原样，未提交。
- 建议：commit 此修复并本地打包验证（`npm run dist:portable` 或仅 `npm run build` + 启动 portable 测试设置面板）。
