# AI 对话 TDZ 错误（`Cannot access 'Ce'/'je' before initialization`）根因定位与修复

**日期**: 2026-08-19
**状态**: ✅ 已修复并验证（打包 → CDP 实测 0 异常）

---

## 一、问题现象

TCIDE 便携版中，AI 对话每次发送都报错：
- `错误: Cannot access 'je' before initialization`
- `错误: Cannot access 'Ce' before initialization`

UI 不崩，但 AI 对话功能完全不可用。

## 二、错误的排查路径（已推翻）

1. **误判为"历史持久化残留"**：曾认为旧坏 bundle 时代的错误气泡被存进 `chatSessions`，用 `clean_chat_errors.js` 清理了 5 条脏气泡，但用户实时复现，结论错误。
2. **误判 bundle 变量**：以为 `Ce`（即源码 `projectRules`）声明带初值 `let Ce=""`、`je` 是函数声明会提升，理论上无 TDZ 窗口。但漏看了模块顶层的执行顺序。

## 三、真实根因（CDP 远程调试定位）

用 CDP（`--remote-debugging-port=9222`）+ `Debugger.setPauseOnExceptions({state:'all'})` 抓到**未捕获异常**：

```
TypeError: window.api.perfGetMetrics is not a function
    at ws (index-*.js:526:1184)
    at (anonymous) (index-*.js:537:3231)   ← 模块顶层
```

**因果链：**

1. `src/renderer/main.ts:6878` 在**模块顶层裸调用** `updateStatusBarPerf()`；
2. `updateStatusBarPerf()`（main.ts:6526）执行 `(window as any).api.perfGetMetrics()`；
3. 但 `perfGetMetrics` 在 `preload.ts` / `ipc-handlers.ts` 里**根本不存在**（perf 模块 IPC 从未接线）；
4. 调用 `undefined()` 同步抛 `TypeError`（`.catch(()=>{})` 接不住**同步**异常）；
5. 该异常中断模块顶层求值；
6. 后面第 537 行的 `let Ce=""`（即源码 `let projectRules: string = ''`）声明**从未执行**；
7. `Ce` 永久停在 TDZ（暂时性死区）；
8. 之后点击发送，`sendToAI` 函数体读 `Ce`（`content:Ce`）→ 报 `Cannot access 'Ce' before initialization`。

**`je` 同理**：是同一类 TDZ 错误，模块顶层多个 `let` 声明（`agentMode`、`mcpToolsEnabled`、`projectRules` 等）都被中断，发送时先读到哪个短变量名就报哪个。

## 四、修复内容

### 核心修复（根治 TDZ）

`src/renderer/main.ts` 两处：

1. **`updateStatusBarPerf()`**（6523 行附近）：改为可选调用 + try/catch 同步保护，缺失 API 时静默跳过：

```ts
function updateStatusBarPerf(): void {
  try {
    const perfGetMetrics = (window as any).api?.perfGetMetrics;
    if (typeof perfGetMetrics === 'function') {
      perfGetMetrics.call((window as any).api).then((m: any) => {
        if (m && m.openCount > 0) { /* metrics available */ }
      }).catch(() => {});
    }
  } catch (_) { /* 静默 */ }
}
```

2. **`perfGcSweep` 定时调用**（6593 行附近）：同样改为可选调用 + 保护。

### 附带修复（`require is not defined`）

`saveSessionsToDisk()`（main.ts:2239）里 `const p = (window as any).path || require('path')` 在 renderer（浏览器环境）中 `require` 未定义，且 `p` 变量实际未使用。已删除该行死代码。

## 五、验证结果

重新构建（vite `index-h-KTZIGz.js`）+ 打包（`release\虎猫 TCIDE 0.0.1.exe`，81294200 字节）+ CDP 实测：

- ✅ **0 次异常暂停**（25s 观察窗），`Ce`/`je` TDZ 彻底消失
- ✅ `require is not defined` 已消失
- ✅ AI 发送链路正常触发

## 六、遗留的次生问题（非致命，均被 catch，不影响 AI 对话）

这些是「renderer 调用了未接线的 IPC API」，属 SUPER_PLAN 记录的 IPC 接线不完整范畴，本轮未处理：

- `window.api.lintFile is not a function`（打开文件时）
- `Cannot assign to read only property 'execCommand'`（preload 的 api 对象只读，AutoHeal 想 monkey-patch 失败）
- `tcide://icon-16.png` CSP 拒绝（图标协议未加进 CSP img-src）
- 其他未接线 API：`perfGetMetrics`/`perfGcSweep`/`contextInit`/`contextStartTrim`/`entropy*`/`warehouse*`/`runner*`/`chunker*`/`batchSearch`/`gitintelGenerateCommitMessage`/`autohealParseErrors`/`formatFile`/`testModelConnection` 等（renderer 调用了但 preload+ipc-handlers 均未实现）

## 七、关键技术要点（后续复用）

1. **CDP 调试 Electron**：`Start-Process` 带 `--remote-debugging-port=9222`，Node 22 有全局 `WebSocket`，可直接写 CDP 客户端。
2. **`Debugger.setPauseOnExceptions({state:'all'})`** 能抓被 catch 的异常，是定位 TDZ 的关键。
3. **vite 压缩短变量名**：`Ce`=projectRules、`je`=确认对话框函数、`U`=mcpToolsEnabled、`Bt`=agentLoopRound。
4. **模块顶层同步抛错会中断后续 let 声明**，导致 TDZ——排查时不能只看变量声明本身，要看声明**之前**的顶层裸调用。
5. **`.catch()` 只能接住 Promise 拒绝，接不住同步抛出的 TypeError**——裸调用缺失 API 必须先判空或 try/catch。
