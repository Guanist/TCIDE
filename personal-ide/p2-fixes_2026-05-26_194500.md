# P2 Fixes: TypeScript Compilation Errors — 2026-05-26 19:45

## Result
22 renderer TS errors → **0 errors**. Main process also 0 errors.
Final: `tsc -p tsconfig.main.json` ✅ / `tsc -p tsconfig.json` ✅

## Changes

### types.d.ts (rewrite)
**Removed** unused interfaces: `UsageSummary`, `ModelUsage`, `ProjectUsage`
**Added** correct usage types matching IPC handlers:
- `UsageToday` (date, inputTokens, outputTokens, totalTokens, costRmb, durationMs, requestCount)
- `UsageTotal` (same + firstSeen, lastSeen)
- `UsageByProject` (projectPath, projectName, totalTokens, costRmb, durationMs, requestCount, lastUsed)
- `UsageByDate` (date + token/cost/duration/count stats)
- `FileTreeNode` interface

**Added** missing TcIdeApi methods:
- `readDirectory`, `abortAI`, `abortTask`, `runBuilder`, `runCoder`
- `execCommand`, `deleteFile`, `openProject`, `getProjectPath`
- `getUsageTotal`, `getUsageByDate`

### main.ts (3 fixes)
1. **Monaco `getValueInRange`** (4 errors → 0):
   - Changed `ed.getValueInRange(selection)` → `ed.getModel()?.getValueInRange(selection) ?? ''`
   - Monaco v0.51+ moved `getValueInRange` to `ITextModel`; editor type no longer has it
   - Added `ed: IStandaloneCodeEditor` type annotations on both action callbacks

2. **ModelConfig type cast** (1 error → 0):
   - Removed `as Record<string, string>` cast on `getModelConfig()` result
   - Uses native `ModelConfig` type (widened provider field)

3. **contextMenuTarget missing `name`** (1 error → 0):
   - Added `name: string` to `contextMenuTarget` and `showContextMenu` parameter types
